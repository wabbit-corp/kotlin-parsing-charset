# Changelog

## 1.3.0

- Current documented release of `kotlin-parsing-charset`.
- Provides compact `CharSet` range storage, set operations, common ASCII and Unicode constants, and
  `CharSetTop` partition refinement.
- Provides topology abstractions for parser and automaton construction.
